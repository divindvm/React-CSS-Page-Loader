import PageLoader from './PageLoader.jsx';
import './PageLoader.scss';

// Ensure CSS is available when the package is imported
export default PageLoader;
export { PageLoader };

// Re-export CSS for manual import if needed
export { default as PageLoaderCSS } from './PageLoader.scss';
